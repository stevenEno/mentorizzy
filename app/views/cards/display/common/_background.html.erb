<div class="card__background">
  <%= image_tag card.image.presence || "", size: 120, data: { upload_preview_target: "image" } %>
</div>

<% if card.closed? %>
  <div class="card__closed">
    <span class="card__closed-title txt-uppercase"><%= card.closure.reason %></span>
    <strong class="txt-uppercase"><%= card.closed_at.strftime("%b %d, %Y") %></strong>
    <span>by <span class="card__closed-by"><%= familiar_name_for(card.closed_by) %></span></span>
  </div>
<% end %>
